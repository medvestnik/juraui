# Getting Started

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Build distributable assets: `npm run build`

Use `data-theme="light|dark|auto"` on `<html>` to control theming.
